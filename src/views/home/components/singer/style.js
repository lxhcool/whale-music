import styled from 'styled-components';

export const RecommendSingerWrapper = styled.div`
  margin-top: 20px;
  .title {
    margin-bottom: 15px;
    font-weight: bold;
  }
  .singer-list {
    display: flex;
    flex-wrap: wrap;
    margin-top: 30px;
    margin: 0 -15px 0;
  }
`